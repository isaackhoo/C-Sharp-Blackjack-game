<Page x:Class="testCsharp.View.SettingsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:testCsharp.View"
    xmlns:theme="clr-namespace:testCsharp.View.Theme"
    mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="SettingsView">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition
                Height="*" />
            <RowDefinition
                Height="*" />
            <RowDefinition
                Height="*" />
        </Grid.RowDefinitions>
        <TextBlock
            Grid.Row="0"
            Name="SettingsTitle"
            Text="Settings"
            Foreground="{x:Static theme:Brushes.Primary}" />
        <Button
            Grid.Row="1"
            Name="TestEventButton"
            Content="test whatever"
            Click="onTestButtonPress" />
        <Button
            Grid.Row="2"
            Name="SettingsBackButton"
            Content="Save and Return"
            Click="onBackToTitle" />
    </Grid>
</Page>
